app = "nicknamebot"
primary_region = "ams"

[build]
  dockerfile = "Dockerfile"

[env]
  DELETE_MESSAGES = "true"

[processes]
  app = "python bot.py"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
